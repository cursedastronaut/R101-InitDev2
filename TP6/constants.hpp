#include <string>

using std::string;

const int MAX_NATURE = 2;

const float POIDS=30; // poids par défaut (en gramme)
const float POIDS_BOITE=20; // poids par défaut (en gramme)
const string NOIR = "noir";
const int CNOIR = 0;
const string BLANC = "blanc";
const int CBLANC = 1;
const string LAIT = "lait";
const int CLAIT = 2; 
const string CLASSIQUE = "classique";